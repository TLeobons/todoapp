import { useContext } from "react"
import { todo } from "context/todoStore";

const  = useContext();

const Todos = () => {
  return (
    <div>
      <input
        value={}
    </div>
  )
}

export default Todos
